#!/bin/sh -e

action="$1"
oldversion="$2"

if [ "$action" != configure ]; then
    exit 0
fi

@echo Kernel installed to /usr/share/kernel/linux-boot.img you need to 'dd' it to the correct boot partition, eg: sudo dd if=/usr/share/kernel/linux-boot.img of=/dev/disk/by-partlabel/boot3
